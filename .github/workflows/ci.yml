name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x, 18.x, 20.x]

    steps:
    - uses: actions/checkout@v3
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test
      env:
        EMAIL_USER: ${{ secrets.EMAIL_USER }}
        EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        NODE_ENV: test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Deploy to Render
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      run: |
        # Cancel any existing deployments
        curl -X GET "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
          -H "accept: application/json" \
          -H "authorization: Bearer $RENDER_API_KEY" | \
        jq -r '.[] | select(.status=="in_progress") | .id' | \
        while read deploy_id; do
          curl -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys/$deploy_id/cancel" \
            -H "accept: application/json" \
            -H "authorization: Bearer $RENDER_API_KEY"
        done
        
        # Trigger new deployment
        curl -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
          -H "accept: application/json" \
          -H "authorization: Bearer $RENDER_API_KEY"

  cancel-pending:
    needs: [test]
    if: failure()
    runs-on: ubuntu-latest
    
    steps:
    - name: Cancel Pending Deployments
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      run: |
        curl -X GET "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
          -H "accept: application/json" \
          -H "authorization: Bearer $RENDER_API_KEY" | \
        jq -r '.[] | select(.status=="in_progress") | .id' | \
        while read deploy_id; do
          curl -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys/$deploy_id/cancel" \
            -H "accept: application/json" \
            -H "authorization: Bearer $RENDER_API_KEY"
        done
